text = "BANANABANANA"
pattern = "ANA"

n = len(text)
m = len(pattern)

positions = []

for i in range(n - m + 1):
    j = 0

    while j < m and text[i + j] == pattern[j]:
        j += 1

    if j == m:
        positions.append(i)

print("Occurrences at positions:", positions)
